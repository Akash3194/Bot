import React, { Fragment } from "react";
import { Box } from "@material-ui/core";

const Profile = () => {
  return (
    <Fragment>
      <Box> </Box>
    </Fragment>
  );
};

export default Profile;
